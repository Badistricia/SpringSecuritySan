# 应用名称
spring:
  application:
    name: TokenDemo
  datasource:
    url: jdbc:mysql://localhost:3306/securitydemo?characterEncoding=utf-8&&serverTimezone=UTC
    username: root
    password: 1234567
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8888
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
